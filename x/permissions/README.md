# x/permissions Migration Marker

Status: deprecated-native-app-logic.

Target: contract permission/capability standard.

Application permission logic should become a contract permission standard.
Protocol authorities remain native in `x/config`, `x/constitution`, and
`x/system-registry`.
