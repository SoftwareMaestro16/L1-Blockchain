# x/workflow Migration Marker

Status: deprecated-native-app-logic.

Target: contract workflow library / SDK tooling.

Workflow behavior is application-level and should move to AVM contracts or SDK
tooling. Keep existing files and tests until contract/tool replacements are
implemented and compatibility-tested.
