# x/dex Migration Marker

Status: deprecated-after-contract-replacement.

This native module is retained for compatibility and must not be removed until
the AMM factory, pool, and LP-token AVM contract replacements are implemented
and pass compatibility tests for swap, add liquidity, remove liquidity, fees,
and deterministic state export/import.
